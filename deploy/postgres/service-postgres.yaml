apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: asw-project
spec:
  type: NodePort
  selector:
    app: instagnam
    service: postgres
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432