apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: postgres-ricette-seguite-deployment
  namespace: asw-project
  labels:
    app: instagnam
    service: postgres-ricette-seguite
spec:
  replicas: 1
  selector:
    matchLabels:
      app: instagnam
      service: postgres-ricette-seguite
  template:
    metadata:
      labels:
        app: instagnam
        service: postgres-ricette-seguite
    spec:
      containers:
      - name: asw-postgres-ricette-seguite
        image: postgres:12.3
        imagePullPolicy: Always
        ports:
          - containerPort: 5432
        envFrom:
          - configMapRef:
              name: postgres-ricette-seguite-config
        volumeMounts:
          - mountPath: /var/lib/postgresql/data
            name: postgresdb-ricette-seguite
      volumes:
        - name: postgresdb-ricette-seguite
          persistentVolumeClaim:
            claimName: postgres-ricette-seguite-pv-claim
